'use client';

import WorkbookCreateDrawer from '@/app/(after-login)/(nav)/workbook/_components/workbook-create-drawer';

const Page = () => {
  return <WorkbookCreateDrawer />;
};

export default Page;
