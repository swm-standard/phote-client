import WorkbookDeleteDialog from '@/app/(after-login)/(top)/workbookDetail/[workbookId]/_components/workbook-delete-dialog';

const Page = () => {
  return <WorkbookDeleteDialog />;
};

export default Page;
