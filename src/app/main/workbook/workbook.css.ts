import { style } from '@vanilla-extract/css';

export const workbookPage = style({
  width: '100%',
  height: '100%',
  backgroundColor: '#fff',
});

export const workbookCard = style({
  width: '80%',
  padding: '10px',
  border: '1px solid black',
});
